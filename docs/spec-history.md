# Historical specification note

This repository started from an internal sprint/specification workflow during early
development.

The source of truth for current behavior is now:

- the implementation under `src/readcast/`
- the tests under `tests/`
- the contributor and user documentation in the repository root and `docs/`

The old sprint planning notes are intentionally not kept as the main project entry point.
